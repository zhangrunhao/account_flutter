import 'package:flutter/material.dart';

class AccountEditPage extends StatelessWidget {
  const AccountEditPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("账户编辑"),
      ),
      body: const Center(
        child: Text("zhangrh"),
      ),
    );
  }
}
